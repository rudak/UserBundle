{% extends 'RudakUserBundle:Email:basic.html.twig' %}

{% block emailContent %}
    <h2>Changement d'adresse e-mail</h2>
    <p class="lead">
        Chez <strong>{{ website }}</strong>, votre adresse email était jusqu'ici <strong>{{ user.email }}</strong>
        et vous la remplacez par <strong>{{ user.emailTmp }}</strong>.
    </p>
    <p class="callout">
        Pour valider cette adresse, vous devez cliquer sur le lien ci dessous. Attention vous avez
        jusqu'au {{ user.securityHashExpireAt|date('d/m/Y \\à H:i:s') }} pour effectuer cette action.
        <br/>
        <a href="{{ link }}">
            Lien de validation de l'adresse email
        </a>
    </p>
{% endblock %}