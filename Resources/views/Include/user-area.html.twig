{% if not app.user %}
    {% render(controller('RudakUserBundle:Form:getFormLogin')) %}
    <div class="userArealinks">
        <p>
            <a href="{{ path('rudakUser_lost_pwd') }}" title="Mot de passe perdu">
                Mot de passe perdu
            </a>
            <br/>
            <a href="{{ path('record_new') }}" title="Enregistrement nouvel utilisateur">
                Enregistrement nouvel utilisateur
            </a>
        </p>
    </div>

{% else %}
    <div class="userArealinks">
        <p>
            Loggé en tant que {{ app.user.username }}.
        </p>

        <p>
            <a href="{{ path('rudakUser_profile') }}" title="Mon profil">
                Mon profil
            </a>
        </p>

        <p>
            <a href="{{ path('logout') }}" title="Deconnexion">
                Deconnexion
            </a>
        </p>
    </div>
{% endif %}